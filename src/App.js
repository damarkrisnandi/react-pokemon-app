import './App.css';
import MainPage from './features/MainPage';

function selectedPokemon(data) {
  console.log(data);
}

function App() {
  return (
    <div>
      <MainPage/>
    </div>
  );
}

export default App;
